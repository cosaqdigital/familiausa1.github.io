{
  "name": "familiausa1-astro-poc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/copy-static-assets.mjs",
    "preview": "astro preview",
    "validate:poc": "node scripts/validate-astro-poc.mjs",
    "extract:pilot": "node scripts/extract-legacy-articles.mjs --pilot",
    "extract:all": "node scripts/extract-legacy-articles.mjs --all"
  },
  "devDependencies": {
    "astro": "^5.0.0"
  }
}
